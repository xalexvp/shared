'use strict';

describe('myApp.basket module', function() {

  beforeEach(module('myApp.basket'));

  describe('basket controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var basketCtrl = $controller('basketCtrl');
      expect(basketCtrl).toBeDefined();
    }));

  });
});